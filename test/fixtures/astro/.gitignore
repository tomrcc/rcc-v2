node_modules
package-lock.json
dist
_untranslated_site
.astro
# base.json is regenerated by `rosey generate` on every build; only the
# hand-crafted locale files are checked in.
rosey/base.json
rosey/base.urls.json
