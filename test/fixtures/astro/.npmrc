# Copy the file: connector into node_modules instead of symlinking, so npm's
# bin chmod hits an owned copy rather than CloudCannon's read-only source mount
# (which fails with EPERM). Harness builds dist first, so still tests local code.
install-links=true
